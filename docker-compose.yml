services:
  hot-sale-scraper:
    build: .
    container_name: hot-sale-scraper
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/prices.db
    volumes:
      - ./data:/app/data
